const config = {
  overrides: [
    {
      files: ['*.css', '*.scss', '*.html', '*.json'],
      options: {
        tabWidth: 2,
        useTabs: false,
      },
    },
  ],
}

export default config
